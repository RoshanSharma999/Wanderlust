<% layout("/layouts/boilerplate.ejs") %>
<body>
    <div class="row justify-content-center">
    <div class="col-12 col-lg-8">
    <h2>Edit your listing</h2>
    <form method="POST" action="/listings/<%= thisListing._id %>?_method=PATCH" novalidate class="needs-validation show-loader" enctype="multipart/form-data">
        <div class="mb-3">
            <label class="form-label">Title</label>
            <input type="text" value="<%= thisListing.title %>" name="listing[title]" required autocomplete="off" class="form-control">
            <div class="invalid-feedback">Required</div>
        </div>
        <div class="mb-3">
            <label class="form-label">Description</label>
            <textarea name="listing[desc]" rows="5" cols="30" required class="form-control"><%= thisListing.desc %></textarea>
            <div class="invalid-feedback">Required</div>
        </div>
        <div class="mb-3">
            <label class="form-label">New Image(only if u want to change)</label>
            <input type="file" accept="image/png, image/jpeg" name="newImage" autocomplete="off" class="form-control">
        </div>
        <div class="mb-3">
            <label class="form-label">Price</label>
            <input type="number" min="0" value="<%= thisListing.price %>" name="listing[price]" required autocomplete="off" class="form-control">
            <div class="invalid-feedback">Required</div>
        </div>
        <div class="mb-3">
            <label class="form-label">Location</label>
            <input type="text" value="<%= thisListing.location %>" name="listing[location]" required autocomplete="off" class="form-control">
            <div class="invalid-feedback">Required</div>
        </div>
        <div class="mb-3">
            <label class="form-label">Country</label>
            <input type="text" value="<%= thisListing.country %>" name="listing[country]" required autocomplete="off" class="form-control">
            <div class="invalid-feedback">Required</div>
        </div>
        <button class="btn btn-danger">Update</button>
    </form>
    </div>
    </div>
</body>
